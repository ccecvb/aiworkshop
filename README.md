# AI Workshop

This is an OpenEdge ABL project for the AI Workshop.
It contains business entities, datasets, and window-based UI components.

## Project Structure

- `src/business/` - Business Entities and Datasets
- `src/*.w` - Window UI components
- `dump/` - Database schema
- `doc/` - Documentation
